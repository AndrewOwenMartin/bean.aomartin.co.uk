# Context Map

This is a multi-context monorepo. Each package has its own domain model.

- [`sdsjs/CONTEXT.md`](sdsjs/CONTEXT.md) — the TypeScript SDS algorithm library: formal taxonomy, swarm representations, variant scope
- [`bean/CONTEXT.md`](bean/CONTEXT.md) — the React educational website: content structure, animation approach, tech decisions
- [`analysis/README.md`](analysis/README.md) — sympy-based mathematical research extending the thesis's DDSM analysis to SDS variants; writes generated LaTeX into `bean/markdown/generated/` for MDX to import
